# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/viajesdb
spring.datasource.username=postgres
spring.datasource.password=16092007mottajgm
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Optional: Kafka Configuration (since you have spring-kafka in your pom.xml)
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=transporte-group
spring.kafka.consumer.auto-offset-reset=earliest

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration